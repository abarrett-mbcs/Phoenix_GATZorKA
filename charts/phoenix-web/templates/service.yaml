apiVersion: v1
kind: Service
metadata:
  name: {{ .values.service.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Chart.name }}
  ports:
    - protocol: TCP
      port: {{ .values.service.port }}
      targetPort: 80
